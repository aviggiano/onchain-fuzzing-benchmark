## onchain-fuzzing-benchmark

1. Run the benchmark: `screen -L -d -m bash script/benchmark.sh`
2. Aggregate the results: `node script/script.js > /tmp/results.csv`
3. Analyse the results: `python script/analyse.py`
